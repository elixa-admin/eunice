{
  "name": "eunice-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --prefix src run dev",
    "build": "npm --prefix src run build",
    "start": "npm --prefix src run start",
    "lint": "npm --prefix src run lint",
    "typecheck": "npm --prefix src run typecheck",
    "check": "npm --prefix src run check",
    "verify:src": "node scripts/verify-workspace.mjs src",
    "verify:dev": "node scripts/verify-workspace.mjs dev"
  }
}
